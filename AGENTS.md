# AI-Engine-Dev ワークスペースガイド

このリポジトリは、AIを活用した資料作成とシステム開発を、成果物・再利用部品・検証手段に分けて管理するためのワークスペースです。

このファイルはリポジトリ全体に適用します。変更対象のディレクトリに、より深い階層の`AGENTS.md`がある場合は、その範囲では局所的な指示を優先します。成果物、検証、関連ドキュメントは同じ変更単位で整合させます。

## フォルダの役割

- `Lerning/`: 初めて参加する人向けの学習資料。作業を始める前に `Lerning/README.md` を読み、担当分野の資料へ進む。
- `presentation/`: 提案書、週次報告、説明資料などの作成領域。詳細な作業ルールは `presentation/AGENTS.md` を参照する。
- `development/`: 要求整理、設計、実装、テスト、障害調査などのシステム開発領域。詳細な作業ルールは `development/AGENTS.md` を参照する。

## 共通方針

1. 作業前に目的、対象読者または利用者、入力情報、期限、完了条件を確認する。
2. 事実、推測、未確認事項を区別し、根拠となる資料や実装を明示する。
3. 成果物は適切なフォルダに置き、再利用できる手順や部品は `skills/`、`scripts/`、`templates/` に分離する。
4. 変更後は対象に応じて内容確認、表示確認、テストを行い、実施済みの確認と未確認事項を分けて報告する。
5. 機密情報、個人情報、認証情報は入力資料や生成物へ残さず、プロジェクト固有のセキュリティ規則を優先する。

## 作業前に読む資料

- 初めてこのワークスペースで作業する場合は、`Lerning/README.md`から担当分野の学習資料へ進む。
- 資料作成に触れる場合は、`presentation/AGENTS.md`と`presentation/README.md`を読む。PowerPointの作成、改訂、Template取込、Design調整では`presentation/skills/README.md`から対応するSkillを選ぶ。
- システム開発に触れる場合は、`development/AGENTS.md`と`development/README.md`を読む。要求、仕様、設計、計画、調査、TDD、実装、障害調査、レビュー、文書化、デリバリーのSkill選択と標準フローは`development/skills/README.md`を正本とする。
- 個別Projectに`AGENTS.md`、`README.md`、要件書、設計書、ADR、検証手順がある場合は、変更前に対象範囲の文書を読む。

## Agent Skills

独自の手順を組み立てる前に、対象工程で利用可能なSkillを確認します。

### 基本ルーティング

- 資料作成では、`presentation/skills/README.md`に従い、Deck作成、Template取込、Template説明、Design System調整から目的に合うSkillを選ぶ。
- システム開発では、`development/skills/README.md`に従い、依頼に必要な工程Skillだけを選ぶ。標準フローを短縮する場合も、受け入れ条件、検証方法、権限の境界は実装前に明確にする。
- 承認済みの設計・実装計画をTask単位で委譲し、実装、二軸レビュー、修正、全体レビューまで連続実行する場合は、`development/skills/orchestrated-development/SKILL.md`を使う。
- バグや性能劣化では、修正を始める前に再現と根本原因の特定を行い、`debugging`または対象環境で利用できる診断Skillを使う。
- 実装変更ではTDDを基本とし、失敗するTestまたは再現可能なFeedback loopを先に作る。完了前に仕様適合性とRepository標準の二軸で差分をレビューする。

### Skill利用の原則

- Skillの手順を`AGENTS.md`へ複製しない。`AGENTS.md`は守る規則と参照条件を定義し、Skillは工程の進め方を定義する。
- 同じ目的のSkillを重複して適用しない。同名Skillが複数ある場合は、`development/skills/README.md`で位置づけとPathを確認する。
- 利用者がSkillを明示した場合は、そのSkillを作業フローへ組み込み、SkillとRepository固有規則の両方を満たす。
- Skillと本ファイルが競合する場合は、本ファイルと、より深い階層の`AGENTS.md`を優先する。
- Skillの利用は、コミット、push、merge、Issue更新、外部送信、デプロイの権限を与えない。依頼内容とProject固有規則で明示された範囲だけを実行する。
- Skillの完了判定だけに依存せず、このファイルと対象領域の完了条件を確認する。

## 作業の進め方

ルート直下の`AGENTS.md`に新しく変更を加える場合は、変更前に新しいブランチを作成して切り替える。

1. `git status --short --branch`で現在のBranchと既存変更を確認し、依頼外の変更を保持する。
2. 目的、対象読者または利用者、入力情報、対象範囲、制約、期限、受け入れ条件、外部変更の権限を確認する。
3. 変更対象の実物と関連文書を照合し、確認済みの事実、推測、未確認事項を分ける。
4. 対象領域のSkill一覧から必要な工程を選び、検証可能な小さな作業単位へ分ける。
5. 成果物と、それを裏付けるTest、検査、出典記録を同じ作業単位で作成または更新する。
6. 実装や成果物で確定した仕様、設計判断、利用手順を正本へ反映し、他の文書は正本へのLinkを使う。
7. 依頼内容への適合性と、Repository・対象領域の規則への適合性を分けてレビューする。
8. 対象に応じたTest、Build、静的検査、表示確認を実行し、実施済みと未実施を分けて記録する。
9. `git diff --check`と変更対象の差分を確認し、依頼外の変更が含まれていないことを確かめる。
10. 変更内容、根拠、検証結果、未確認事項、残るRisk、文書影響を完了報告にまとめる。

## ドキュメント同期

すべてのファイル変更で、README、一覧、利用手順、説明資料、学習資料への影響を確認する。影響する文書は同じ変更内で更新し、説明の正本は一か所に置いて他の文書からリンクする。

- 利用者向けの機能、構成、入口、主要な使い方を変更した場合は、ルートまたは対象領域の`README.md`を更新する。
- ファイル、フォルダ、Skill、Template、Scriptを追加・削除・改名した場合は、対象を掲載する一覧または最も近い`README.md`を更新する。
- セットアップ方法、コマンド、前提条件、利用手順を変更した場合は、対応するセットアップまたは説明用文書を更新する。
- 初めて参加する人が学ぶ内容、推奨手順、標準フローを変更した場合は、`Lerning/README.md`と該当する学習資料を更新する。
- Agentの作業規則や参照先を変更した場合は、該当範囲の`AGENTS.md`を更新する。`CLAUDE.md`は、そこから該当する`AGENTS.md`を参照できない場合に更新する。
- 外部由来のSkill、ライセンス、参照Revisionを変更した場合は、該当する案内文書と`THIRD_PARTY_NOTICES.md`を更新する。

### フォルダREADME

フォルダを追加した場合、または既存フォルダの役割や使い方を変更した場合は、そのフォルダにREADMEが必要か判定する。次のいずれかに該当するフォルダには`README.md`を作成または更新する。

- 独立した目的、責務、ワークフローを持つ。
- 利用者がファイルや成果物を選択するための一覧や説明を必要とする。
- セットアップ、入力、出力、実行方法、検証方法の説明を必要とする。
- 複数の再利用部品や、継続的に追加される成果物を含む。
- 親フォルダの説明だけでは、初めて利用する人が次の行動を判断できない。

READMEには、必要な範囲で目的、対象読者、主な内容、使い方、検証方法、関連文書へのリンクを記載する。素材だけを置くLeaf folder、生成物専用フォルダ、単一の`SKILL.md`や`AGENTS.md`が入口として十分なフォルダは、親READMEまたは一覧から役割を確認できる場合に限りREADMEを省略できる。

### 完了条件

- 依頼の受け入れ条件を満たす成果物があり、根拠と変更範囲を説明できる。
- 対象領域で必要なSkillまたは同等の標準フローを適用し、省略した工程には理由がある。
- 仕様適合性とRepository標準の二軸で差分を確認している。
- 対象に応じたTest、Build、静的検査、表示確認を実行し、成功した確認と未実施の確認を区別している。
- 変更した名称、パス、コマンド、構成が、README、一覧、説明資料、学習資料で一致している。
- 新規または変更したフォルダについて、READMEの作成要否を判定している。
- ローカルリンク、記載したコマンド、参照先を確認している。
- `git diff --check`が成功し、依頼外の変更が保持されている。
- 作業報告に`文書影響: <更新した文書と理由>`または`文書影響なし: <更新不要と判断した理由>`を記載している。

## 作業開始時の参照先

- 資料作成を学ぶ場合: `Lerning/01_presentation.md`
- システム開発を学ぶ場合: `Lerning/02_system-development.md`
- AIを使った共通の進め方を学ぶ場合: `Lerning/03_ai-assisted-workflow.md`
