# AI-Engine-Dev Claude Code Guide

作業開始時に`Lerning/README.md`を読み、資料作成では`presentation/AGENTS.md`、システム開発では`development/AGENTS.md`に従う。

承認済みの設計・実装計画を、Opusによる設計・レビューとSonnetによる実装へ分担するときは、`development/skills/orchestrated-development/SKILL.md`を使用し、`.claude/agents/`の役割定義を呼び出す。

SkillやAgentの利用はCommit、push、merge、外部更新、デプロイの権限を追加しない。利用者の依頼とプロジェクト固有の規則で明示的に許可された操作だけを行う。
